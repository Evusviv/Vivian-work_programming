#include <stdio.h>

/**
 * main - Entry point
 * Return: 0
 */
int main()
{
  char c = 'a';
  char d = 'z';

  while(c <= d)
      {
	if((c != 'q') && (c != 'e'))
	  {
	    putchar(c);
	    c++;
	  }
      }
  
  putchar('\n');
  return 0;
}
