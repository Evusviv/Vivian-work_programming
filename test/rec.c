#include <stdio.h>
func_area(int length, int breadth)
{
int length;
int breadth;
int area;
area = length * breadth;
return(area);
int main()
{
length = 8;
area = 4;
printf("The area of the rectangle is %d\n", length, breadth, area);
return(area);
}
